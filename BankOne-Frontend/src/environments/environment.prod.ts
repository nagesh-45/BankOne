export const environment = {
  production: true,
  // Replace at build time via file replacement, or set before prod build.
  // Example: https://bankone-api.onrender.com
  apiBaseUrl: 'https://YOUR-BACKEND-URL'
};
